apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: k8s-swarm-role
  namespace: swarm
rules:
  - apiGroups:
      - apps
    resources:
      - pods
      - statefulsets
    verbs:
      - get
      - watch
      - list
      - patch
  - apiGroups: [""]
    resources:
      - pods
      - statefulsets
      - configmaps
    verbs:
      - get
      - watch
      - list
      - update

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: k8s-swarm-rbac
  namespace: swarm
subjects:
  - kind: ServiceAccount
    name: default
    namespace: swarm
roleRef:
  kind: ClusterRole
  name: k8s-swarm-role
  apiGroup: rbac.authorization.k8s.io

